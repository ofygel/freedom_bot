{
  "name": "freedom_bot",
  "version": "1.1.0",
  "description": "Telegram delivery aggregator bot for Almaty (cleaned & fixed).",
  "main": "dist/index.js",
  "type": "commonjs",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "ts-node --transpile-only src/index.ts",
    "check": "tsc --noEmit",
    "test": "node --require ts-node/register --test --test-concurrency=1 tests/*.test.ts"
  },
  "dependencies": {
    "dotenv": "^16.4.0",
    "telegraf": "^4.15.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.17",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  }
}
